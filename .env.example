# Database connection (PostgreSQL)
DATABASE_URL="postgresql://protegius:protegius_secret_2026@localhost:5432/protegius_crm?schema=public"

# Auth Secret (Minimum 32 characters for HMAC SHA-256 JWT)
AUTH_SECRET="protegius_super_secret_jwt_key_32_bytes_min_length"

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
